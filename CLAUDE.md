# Gup Gup (줍줍) — 프로젝트 스펙

한국에 거주하는 외국인(특히 동남아시아·중앙아시아 출신)을 위한 커뮤니티 웹앱.
정식 서비스 출시를 목표로 하며, 사용자 게시글 중심의 커뮤니티형 서비스다.

## 기술 스택
- 프론트엔드: **Next.js** (React 기반, SSR/SEO 고려)
- 백엔드/DB/인증/스토리지: **Supabase**
- 형태: 네이티브 앱이 아닌 **웹앱** (모바일 브라우저 우선, PWA는 추후 검토)

## 카테고리 구조
| 대분류 | 하위/메인 노출 | 비고 |
|---|---|---|
| 뉴스 | 스크롤형 뉴스, 메인에 최신글 10개(대형 박스) | 작성자 아바타 없음 (운영자 콘텐츠) |
| 커뮤니티 | 자유게시판 / 한국생활 QnA / 여행·맛집추천, 메인 최신글 10개(대형 박스) | |
| 주택 | 하위 항목 미정, 메인 최신글 5개 | TBD |
| 한국 생활 | 결혼과 육아 / 식당 / 병원 / 한국 핸드폰 / 서류·행정(외국인 증명서, visa guide) / 한국법, 메인 최신글 5개 | 기존 "Living"에서 명칭 변경 |
| 중고거래 | 메인 최신글 5개 | 상세화면 본문 하단 쪽지 버튼 별도 |
| 한국 대학교 | 메인 최신글 5개 | |
| 만남 | 자유 포스팅(동네 친구·소모임 구함, 이성친구 구함 등), 메인 최신글 5개 | 매칭/좋아요 기능 없음. 상세화면 본문 하단 쪽지 버튼 별도 |
| 문의사항 | 메인 박스 없음, 헤더 메뉴바에만 존재 | 비공개 1:1 문의, 국가 태그 없음 |

메인 화면 레이아웃: 뉴스·커뮤니티는 2열 대형 박스, 나머지 5개 카테고리(주택/한국생활/중고거래/한국대학교/만남)는 3열 그리드 일반 박스.

## 다국어·번역
- 기본 언어: 한국어
- 헤더 우측 지구본 아이콘 → UI 텍스트(카테고리명, 안내 문구 등)만 다국어 전환
- 게시글 본문은 작성 언어 원문 그대로 유지 (자동번역 없음)
- 게시글 상세 화면에서 작성자 닉네임 옆 "번역 보기" 버튼 클릭 시에만 번역 API로 간단 번역 표시 (원문/번역 토글)
- 대상 국가(및 국가 필터 목록): 베트남, 태국, 라오스, 인도네시아, 미얀마, 필리핀, 몽골, 기타 (+ "전체")
  - "중앙아시아"는 별도 국가로 분리하지 않고 "기타"로 통합

## 국가 필터 & 게시판 목록
- 게시글 작성 시 국가 필드는 필수, 회원가입 시 설정한 국가가 기본값으로 자동 입력(수정 가능)
- 목록 상단에 국가 필터 칩(전체/베트남/태국/라오스/인도네시아/미얀마/필리핀/몽골/기타), 라디오식 단일 선택, 기본값 "전체"
- 리스트 항목: 작성자 아바타, 제목, 국가, 작성자 닉네임, 작성일, 조회수, 댓글수, (사진 첨부 시) 썸네일
- 정렬: 최신순 / 인기순(조회수·댓글수 기준)
- 하단: 페이지네이션(20개/페이지, 현재 페이지 강조) + 카테고리 내부 검색창(검색범위 드롭다운[제목+내용 등] + 검색어 입력 + 돋보기 버튼)
- 게시글이 없는(문의사항 제외) 모든 카테고리는 같은 목록 UI 패턴 공유

## 게시글 상세 화면
- 상단: 국가 태그, 작성일, 조회수
- 작성자 아바타 + 닉네임, 옆에 번역 버튼
- 본문 + 댓글 영역 (대댓글/답글 지원)
- 댓글은 게시글과 별개로 개별 신고 가능
- 게시글 자체의 별도 "신고" 버튼 없음 — 작성자 닉네임 클릭 시 공통 팝업 사용
- "중고거래", "만남" 카테고리는 본문 하단에 쪽지 보내기 버튼 추가 노출

## 닉네임 클릭 공통 팝업 (전 화면 공통 컴포넌트)
어디서든 상대방 닉네임을 클릭하면 뜨는 팝업, 메뉴 순서 고정:
1. 프로필 보기
2. 쪽지 보내기
3. 차단하기
4. 신고하기

신고: 별도 페이지 없이 모달에서 사유 선택 → 제출 후 "신고가 접수되었습니다" 간단 확인 메시지.

## 회원가입 / 로그인
- 로그인 수단: 구글, 페이스북, 이메일(3종)
- 열람은 비로그인 가능. 글쓰기·쪽지 보내기는 로그인 필요
- 이메일 가입: 비밀번호 암호화 저장, 비밀번호 재설정, 이메일 인증(스팸 방지) 필요
- 이메일 미인증 상태에서는 글쓰기·쪽지 보내기 모두 제한
- 아이디 찾기 기능 없음 (이메일 주소 = 로그인 아이디, 분실 시 "문의사항"으로 유도)
- 최초 로그인 시 온보딩: 닉네임(중복 확인 필수) + 국가 설정. 이후 프로필 페이지에서 변경 가능
- 소셜 로그인의 실명/프로필사진은 노출하지 않고, 별도 설정한 닉네임만 커뮤니티에 표시
- 프로필 이미지: 업로드 가능. 미설정 시 무료 저작권 기본 캐릭터 이미지 중 선택. 뉴스를 제외한 모든 화면(게시판 리스트, 상세, 댓글, 쪽지함, 닉네임 팝업, 프로필)에 공통 표시

## 프로필 페이지
- 내 프로필: 닉네임/국가/이메일, 닉네임·국가·비밀번호 변경, 내 글/댓글/받은쪽지 통계, 로그아웃/회원탈퇴
- 타인 프로필: 닉네임/국가/가입일(공개), 쪽지 보내기/신고하기, 작성 글 목록
- 회원 탈퇴 시 게시글·댓글은 삭제하지 않고 유지, 작성자 표시만 "탈퇴한 회원입니다"로 대체
- "받은 쪽지" 통계 카드 클릭 → 쪽지함으로 이동

## 쪽지함
- 대화 목록 화면과 대화(채팅) 화면을 별개 페이지로 분리 (모바일 기준)
- 대화 화면에서 상대 닉네임 클릭 시 공통 팝업(프로필/쪽지/차단/신고) 동일 적용

## 문의사항
- 비공개 1:1 문의 (작성자 본인과 운영자만 열람), 국가 태그 없음
- 화면: 내 문의 목록(제목 + 답변대기/답변완료 상태 배지 + 새 문의하기 버튼) → 상세는 쪽지함과 동일한 대화형 UI 재사용, 상대는 "Gup Gup 운영팀"

## 알림
- 새 쪽지·문의 답변은 헤더의 "내정보" 영역과 드롭다운 메뉴에 배지(숫자)로만 표시
- 브라우저 푸시 알림, 이메일 알림은 도입하지 않음

## 헤더 & 내비게이션 (전 페이지 공통, sticky)
- 1행: 좌측 로고 / 중앙 검색창(카테고리 선택 드롭다운 + 검색어 입력 + 돋보기 버튼) / 우측 지구본(언어) + 내정보(로그인 전 "로그인" 버튼, 후 닉네임+배지 → 클릭 시 내 프로필/쪽지함/문의사항/로그아웃 드롭다운)
- 2행: 카테고리 메뉴바 (뉴스/커뮤니티/주택/한국 생활/중고거래/한국 대학교/만남/문의사항), 빨간 배경
- 헤더+메뉴바는 모든 페이지에서 스크롤 시에도 상단 고정(sticky)

## 비주얼 디자인
- 배경: 흰색
- 로고: 작은 빨간 사각 배지 안 손(줍는 동작) 아이콘 + "Gup Gup" 워드마크 + 작은 "줍줍" 서브텍스트
- 카테고리 메뉴바: 빨간 배경 풀 메뉴바, 흰 텍스트
- 카테고리 박스: 테두리 회색, 박스 안 카테고리 제목은 흰 배경 + 빨간 테두리 + 빨간 텍스트의 작은 뱃지
- 알림/배지: 노란 배경 + 검정 텍스트
- 검색창: 빨간 테두리, 검색 버튼은 텍스트 대신 돋보기 아이콘, 인접 요소(내정보 버튼 등)와 높이 통일
- 페이지네이션: 현재 페이지는 빨간 배경 강조, 나머지는 회색 테두리

## "만남" 카테고리 정책
- 매칭/좋아요 등 데이팅 앱 기능 없음, 단순 자유 포스팅
- 안전장치: 신고·차단·쪽지 기능으로 개인정보 노출 최소화
- ⚠️ 이용약관, 신고 처리 프로세스, 로그 보관 등 법적 리스크 관리는 별도 법률 자문 필요 (이 문서는 법률 자문 대체 불가)

## 아직 미정 (TBD)
- "주택" 카테고리 하위 항목
- 게시글 작성 화면 상세 UI
- 신고 접수 후 운영자 처리 프로세스 및 운영 정책 문서화

## 구현 우선순위 제안
1. Supabase 프로젝트 설정 (Auth: Google/Facebook/Email, DB 스키마, Storage)
2. 공통 레이아웃: 헤더(로고/검색/언어/내정보) + sticky 카테고리 메뉴바
3. 회원가입/로그인 + 온보딩(닉네임/국가) 플로우
4. 메인 화면 (카테고리 박스 그리드)
5. 게시판 목록 화면 (국가 필터, 페이지네이션, 검색) — 카테고리 공통 컴포넌트로 설계
6. 게시글 작성/상세/댓글
7. 닉네임 클릭 공통 팝업(프로필/쪽지/차단/신고) + 신고 모달
8. 프로필 페이지, 쪽지함
9. 문의사항
10. 다국어 UI 전환, 게시글 번역 버튼

## 뉴스 다이제스트 요약 요청 (운영 지침 — Claude용)

사용자가 새 세션에서 "요약해줘", "오늘자 요약해줘", "뉴스 요약해줘" 같은 짧은 요청을 하면, 별도 설명 없이 아래 절차를
그대로 수행할 것. (배경: `news-digest/` 폴더에 매일 네이버 뉴스/블로그/카페 검색 결과를 모아 하루 한 페이지로
정리하는 파이프라인이 있고, Windows 작업 스케줄러가 로그온 시 `news-digest/fetch.mjs`를 자동 실행해 그날의
`news-digest/daily/raw-<KST 날짜>.json`을 미리 만들어둔다. 클라우드 예약 작업/Cowork은 이 환경에서 GitHub
연동이 막혀 있어 쓸 수 없어서, 이 수동 트리거 방식으로 대체했다.)

1. `news-digest/daily/`를 스캔해서 `raw-<날짜>.json`은 있는데 같은 날짜의 `<날짜>.md`가 아직 없는 날짜를
   전부 찾을 것 (며칠 건너뛰었으면 여러 날짜가 나올 수 있음 — 데이터는 지워지지 않고 쌓여 있으므로 전부 처리).
   - 오늘 KST 날짜의 `raw-<날짜>.json`이 아예 없으면 (작업 스케줄러가 아직 안 돌았거나 실패한 경우) 먼저
     `node --env-file=.env.local news-digest/fetch.mjs`를 직접 실행해서 만들 것.
2. `news-digest/seen-topics.json`과 `news-digest/README.md`의 "반복 기사 억제 규칙" 섹션을 읽고,
   `news-digest/daily/`의 최근 날짜별 `.md` 파일 1~2개를 형식 참고용으로 확인.
3. 1번에서 찾은 미처리 날짜들을 **오래된 날짜부터 순서대로** 하나씩 처리. 각 날짜의 raw json에서 핵심
   뉴스/정책/실무정보/국가별 소식/대학·유학생 모집/관광·맛집·할인/한국어교육·입국전준비 등을 주제별로 선별해
   `news-digest/daily/<해당 날짜>.md`로 작성:
   - 원문 전문 복사 금지. 제목 + 1~2문장 핵심 요약 + 원문 링크만.
   - 사실관계 불확실·출처 불분명·개인 신변잡기/광고성 카페 글 제외.
   - 뉴스/정책성 사안이 `seen-topics.json`에서 이미 count≥5면 새 내용 업데이트가 없는 한 요약에서 제외
     (검색 자체는 계속하되 노출만 억제). 새로운 구체적 내용이 있으면 포함하고 summary 갱신 + count를 1로 리셋.
   - 새로 다룬 뉴스/정책성 사안은 `seen-topics.json`에 새 항목으로 추가(count=1). 블로그 개인 후기/팁은 추적 대상 아님.
   - 여러 날짜를 한꺼번에 처리할 때, 뒤 날짜에서 앞 날짜와 같은 사안을 다시 보게 되면 그것도 반복으로 쳐서
     count를 계속 누적할 것 (하루 걸러 요약해도 반복 억제 규칙이 정상 작동하도록).
4. `news-digest/seen-topics.json` 갱신본 저장 (모든 처리 날짜 반영 후 한 번에).
5. **보관 기간(60일) 정리**: `news-digest/daily/`에서 파일명의 날짜가 오늘 KST 기준 60일보다 오래된
   `<날짜>.md`가 있으면 `git rm`으로 삭제할 것 (raw json은 `fetch.mjs`가 실행될 때 자체적으로 60일 지난
   것을 알아서 지우므로 여기서 신경 쓸 필요 없음).
6. 새로 만든 `news-digest/daily/<날짜>.md` 전부, 5번에서 삭제한 파일, `news-digest/seen-topics.json`을
   git add(삭제분은 git rm) + commit. **push는 매번 먼저 사용자에게 물어보고 진행할 것** (자동 push 아님 —
   매번 재확인).
7. 완료 후 처리한 날짜별로 핵심 내용을 대화창에 간단히 요약해서 보여줄 것 (파일 전체를 다시 붙여넣지 말고
   날짜별 하이라이트만). 60일 지나서 삭제한 파일이 있으면 그것도 짧게 언급할 것.
